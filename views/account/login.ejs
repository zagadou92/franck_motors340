<% if (title) { %>
<h1><%= title %></h1>
<% } else { res.redirect('/') } %> <%- messages() %>
<br />
<% if (errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <% }) %>
</ul>
<br >
<% } %>
<div class="form-container">
  <form id="loginForm" action="/account/login" method="post">
    <div class="form-group">
      <label for="email">Email:</label>
      <input
        type="email"
        id="email"
        name="account_email"
        required
        placeholder="Enter a valid email address"
        value="<%= locals.login_email %>"
      >
    </div>
    <div class="form-group">
      <label for="password">Password:</label>
      <input
        type="password"
        id="password"
        name="account_password"
        required
        placeholder="Enter a valid password"
        pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$"
      >
    </div>
    <div class="form-advice">
        <span
          >Passwords must be at least 12 characters and contain at least 1
          number, 1 capital letter and 1 special character</span
        >
    </div>
    <button id="pwd-visibility" type="button">
      Toggle Password Visibility
    </button>
    <button type="submit" class="btn-account">Login</button>
  </form>
  <p id="no-account">
    No account? <a href="/account/register" id="registerLink">Sign-up</a>
  </p>
</div>
